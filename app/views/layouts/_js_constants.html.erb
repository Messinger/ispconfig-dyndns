
<% content_for :additional_javascripts do %>
<script>

  var Constants;
  Constants = (function() {
    Constants.LOGOUT_PATH = '<%= send("destroy_#{current_account.class.name.underscore}_session_path") unless current_account.nil? %>';
    Constants.LOGIN_PATH = '<%= user_session_path %>';
    Constants.current_user = <%= current_user_value %>;
    Constants.root_path = '<%= root_path %>';
    function Constants() {}
      return Constants;
  })();
  window.Constants = Constants;
  window.flash_errors = <%= flash.as_json %>;

</script>

<% end %>
